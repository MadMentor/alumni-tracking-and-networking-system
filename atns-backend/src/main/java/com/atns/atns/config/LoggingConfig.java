package com.atns.atns.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class LoggingConfig {
    // Logging configuration will be handled by application.properties
    // and logback-spring.xml for better flexibility
} 